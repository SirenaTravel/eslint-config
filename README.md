# ips-eslint-config

## Quick start
```npm i @ips/eslint-config -D```
of
```yarn add @ips/eslint-config -D```

## Configuration

Create ```.eslintrc``` 
```
{
  "parser": "babel-eslint",
  "extends": ["ips-config"],
}
```
